# PROMPTS

> 작업 유형별 요청 템플릿. `[ ]` 부분만 채워서 그대로 붙여넣으면
> Claude가 매번 컨텍스트를 다시 추론하지 않고 바로 작업 가능.
> 모든 템플릿 공통: RULES.md의 "출력 규칙"을 따르므로 별도로 재요청할 필요 없음.

## 기능 추가 템플릿
```
[기능명]을 추가해줘.

- 대상 파일: [App.tsx / AdminPage.tsx / 신규 컴포넌트]
- 위치: [예: GallerySection 다음]
- 데이터 필요 여부: [WeddingInfo에 필드 추가 필요 / 불필요]
- 참고 문서: ARCHITECTURE.md의 "패키지 구조"만 참조, 전체 코드 재탐색 금지
- 출력: 변경 diff만
```

## 버그 수정 템플릿
```
버그: [현상을 한 문장으로]

- 재현 경로: [/ 또는 /admin, 어떤 탭/섹션]
- 예상 동작: [ ]
- 실제 동작: [ ]
- 의심 파일(있으면): [ ]

먼저 원인 파일/라인만 Grep으로 특정한 뒤 알려주고, 승인하면 수정할 것.
추측성 광범위 수정 금지.
```

## 리팩토링 템플릿
```
[대상 파일/컴포넌트]를 리팩토링해줘.

- 범위: [특정 함수/컴포넌트만 — 파일 전체 금지]
- 목적: [가독성 / 중복 제거 / 성능 등 1개만]
- 기능 변경 없음 (동작 동일해야 함)
- RULES.md 코드 스타일 유지, 새 라이브러리 도입 금지
```

## API 생성 템플릿
```
> 이 프로젝트는 백엔드가 없는 정적 SPA (ARCHITECTURE.md 참조).
> "API"는 실제로는 localStorage 기반 로컬 함수를 의미함.

[리소스명] 관련 로컬 CRUD 함수를 data/wedding-info.ts에 추가해줘.

- 타입: interface [ ]Entry
- STORAGE_KEYS에 키 추가
- load[X]() / save[X]() 함수 쌍으로, 기존 loadRsvps/saveRsvps 패턴 그대로 따름
```

## 테스트 작성 템플릿
```
[대상 함수/컴포넌트]에 대한 테스트를 작성해줘.

- 이 프로젝트에는 아직 테스트 러너 미설정 — 먼저 설정 필요 여부를 물어볼 것
  (Vitest 도입 시 RULES.md "사용 기술 제한"에 따라 사전 확인 필수)
- 순수 함수(load/save, 데이터 변환)부터 우선 작성
```

## DB 변경 템플릿
```
> 실제 DB 없음 — 이 템플릿은 localStorage 스키마(WeddingInfo 등) 변경을 의미.

[필드명]을 WeddingInfo에 [추가/변경/삭제]해줘.

- 타입: [ ]
- 기본값(defaultWeddingData): [ ]
- 영향받는 화면: [App.tsx의 어떤 섹션] / [AdminPage.tsx의 어떤 Editor]
- 기존 고객 localStorage에 저장된 구버전 데이터와의 하위호환 필요 여부: [예/아니오]
```
