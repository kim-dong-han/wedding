# RULES

> 모든 작업 전 Claude가 먼저 읽어야 하는 규칙. 이 파일은 항상 로드되므로
> 짧게 유지한다 (150줄 이내 목표). 상세 배경 설명은 절대 추가하지 말 것.

## 개발 규칙
- 새 파일/새 추상화를 만들기 전에 기존 파일에 추가할 수 있는지 먼저 확인
- `src/data/wedding-info.ts`는 데이터 스키마의 단일 진실 공급원(SSOT) — 타입 변경은
  여기서만, 다른 곳에 타입 재정의 금지
- 백엔드/DB/외부 API를 새로 도입하는 제안은 하지 말 것 (ARCHITECTURE.md 참조,
  의도적으로 서버리스/localStorage 구조 유지 중)
- 컴포넌트는 필요할 때만 분리. `App.tsx`/`AdminPage.tsx` 내부 함수형 컴포넌트
  패턴(`const Xxx = (...) => {}`)을 그대로 따름 — 새 디렉토리 구조 제안 금지

## 코드 스타일
- TypeScript strict, `any` 금지
- 함수형 컴포넌트 + hooks만 사용 (class 컴포넌트 금지)
- Tailwind 유틸리티 클래스로 스타일링, 인라인 `style={{}}` 및 별도 CSS 파일 추가 금지
  (`index.css`는 전역 리셋용, 컴포넌트 스타일 추가 금지)
- import 순서: 외부 라이브러리 → 내부 모듈(`../data`, `./components`) → 타입
- 들여쓰기 2칸, 세미콜론 생략 스타일(기존 코드 컨벤션 그대로 따름)
- 아이콘은 `lucide-react`만 사용

## 사용 기술 제한
- 패키지 추가 시 반드시 먼저 물어볼 것 (특히 상태관리 라이브러리, UI 프레임워크,
  백엔드 SDK — 이 프로젝트는 의존성 최소화가 목표)
- 허용된 스택 외 기술(Redux, GraphQL, Next.js 전환, ORM 등) 제안 금지
- 이미지/미디어는 `public/`에 직접 배치, CDN·업로드 서비스 연동 제안 금지

## 출력 규칙 (토큰 절약)
- **변경된 부분만** diff/patch 형태로 출력. 파일 전체 재출력 금지
- 요청받지 않은 리팩토링/최적화/주석 추가 금지
- 코드 설명은 변경 이유가 비직관적일 때만 1~2줄. 정상 동작 설명 금지
- 여러 파일 수정 시 파일별로 무엇을 바꿨는지 목록만, 상세 서술 금지

## Git 작업 규칙
- 파일이 수정/보완되어 작업 단위가 완료되면 별도 확인 없이 자동으로
  `git add` → `git commit`(한국어 메시지) → `git push origin main` 수행
- 커밋은 의미 있는 작업 단위로 묶고, 매 파일 저장마다 잘게 쪼개지 않음
- 민감정보(계좌 등 개인정보) 커밋 전 노출 여부만 한 번 확인

## Claude 응답 규칙
- 답변은 한국어, 간결체
- 작업 시작 전 관련 없는 파일까지 미리 다 읽지 말 것 — 필요한 파일만 그때그때 Read
- 큰 파일(`App.tsx`, `AdminPage.tsx`)은 Grep으로 대상 블록 위치를 먼저 찾고
  해당 라인 범위만 Read (offset/limit 사용)
- 같은 세션 내에서 이미 확인한 내용은 재확인·재요약하지 말 것
- 작업 완료 후 "무엇을 했는지" 장문 요약 금지 — 변경 파일 목록 + 한 줄 결과만
- 불확실하면 추측해서 코드 생성하지 말고 질문할 것 (특히 계좌/개인정보 등
  고객 민감 데이터 다루는 `wedding-info.ts` 수정 시)
